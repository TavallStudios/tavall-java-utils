name: Synchronize canonical Java utils

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: java-utils-aggregate-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check out aggregate
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Advance canonical direct utility pointers
        shell: bash
        run: |
          set -euo pipefail
          git submodule sync --recursive
          git submodule update --init --remote
          git submodule update --init --recursive

      - name: Commit aggregate update
        shell: bash
        run: |
          set -euo pipefail

          if git diff --quiet --submodule=short -- . ':!.github/workflows/sync-java-utils.yml'; then
            echo "Aggregate already matches all canonical direct Java utility main branches."
            exit 0
          fi

          git config user.name "Tavall Java Utils Sync"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Changed: Synchronize canonical Java utils" \
            -m "Reason:
          - Keep the Tavall Studios utility aggregate aligned with authoritative direct Java utility repositories.

          Changes:
          - Advance changed direct Java utility submodule pointers to their canonical main revisions.
          - Initialize nested descendants at revisions pinned by their owning aggregate.

          Validation:
          - Git submodule update completed successfully for every configured direct utility."
          git push origin HEAD:main
